<!-- res/drawable/border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Warna isi latar -->
    <stroke
        android:width="1dp"
        android:color="@color/biruGelap"/> <!-- Warna garis tepi -->
    <corners android:radius="8dp"/> <!-- Sudut lengkung opsional -->
</shape>
